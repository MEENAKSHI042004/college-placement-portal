echo [phases.setup]>nixpacks.toml
echo nixPkgs = ["python310"]>>nixpacks.toml
echo.>>nixpacks.toml
echo [start]>>nixpacks.toml
echo cmd = "python manage.py migrate && gunicorn placement_portal.wsgi --bind 0.0.0.0:$PORT --log-file -">>nixpacks.toml